spring:
  application:
    name: api-gateway
  cloud:
    config:
      uri: "http://localhost:10000"
      profile: dev
#eureka:
#  instance:
#    instance-id: "${spring.application.name}:${random.uuid}"
#    hostname: localhost
#    non-secure-port: 8761
#    prefer-ip-address: true
#  client:
#    service-url:
#      defaultZone: "http://${eureka.instance.hostname}:${eureka.instance.non-secure-port}/eureka"
#
#spring:
#  application:
#    name: "api-gateway"
#  config:
#    import: configserver:http://config-server:10000
#  cloud:
#    config:
#      uri: "http://localhost:10000"
#      profile: dev